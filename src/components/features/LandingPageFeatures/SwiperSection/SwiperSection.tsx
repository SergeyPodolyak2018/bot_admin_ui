import styles from './swiperSection.module.scss';

export const SwiperSection = () => {
  return (
    <section className={styles.container}>
      <div>
        <p className={styles.subTitle}>Craft your personal AI</p>
        <p className={styles.title}>Assistant </p>
      </div>
    </section>
  );
};
